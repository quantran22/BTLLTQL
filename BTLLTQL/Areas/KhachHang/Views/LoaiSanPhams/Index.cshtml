@model IEnumerable<BTLLTQL.Models.LoaiSanPham>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/KhachHang/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MaLoaiSanPham)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MaLoaiSanPham)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TenLoaiSanPham }) |
            @Html.ActionLink("Details", "Details", new { id=item.TenLoaiSanPham }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TenLoaiSanPham })
        </td>
    </tr>
}

</table>
