@model IEnumerable<BTLLTQL.Models.NhaSanXuat>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MaNhaSanXuat)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MaNhaSanXuat)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TenNhaSanXuat }) |
            @Html.ActionLink("Details", "Details", new { id=item.TenNhaSanXuat }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TenNhaSanXuat })
        </td>
    </tr>
}

</table>
